PORT=5000
JWT_SECRET=replace_me_with_a_long_random_string
AI_SERVICE_URL=http://localhost:5001

# PostgreSQL — optional. If unreachable, the backend falls back to an in-memory mock DB.
DB_USER=postgres
DB_HOST=localhost
DB_NAME=sports_odds
DB_PASSWORD=postgres
DB_PORT=5432
